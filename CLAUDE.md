<!--
Claude Code reads CLAUDE.md rather than AGENTS.md, so this file exists only to
load the canonical contract. Claude Code expands the import below into context
at session start and strips this comment, so the indirection costs nothing.

Keep the import a bare line: imports inside backticks or code fences are
ignored. Any tool that reads this file without expanding imports should read
AGENTS.md directly.

Repository instructions belong in AGENTS.md. Add content here only for
something genuinely specific to Claude Code. scripts/check-docs-links.sh
verifies that this import is still present.
-->

@AGENTS.md
