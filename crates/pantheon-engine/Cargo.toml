[package]
name = "pantheon-engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
# Configuration semantics (compilation, identity) and the durable authority
# this crate publishes from. Both edges are allowed by
# `docs/development/implementation.md` and are declared because code needs them.
pantheon-core = { path = "../pantheon-core" }
pantheon-store = { path = "../pantheon-store" }

[lints]
workspace = true
